{
  "name": "registry-contract-solidity",
  "version": "0.1.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:live": "hardhat run scripts/test-live.js --network polkadotTestnet",
    "deploy": "hardhat run scripts/deploy.js --network polkadotTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.22.0"
  }
}
